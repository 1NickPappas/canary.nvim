[project]
name = "my-python-project"
version = "0.1.0"
dependencies = [
    "requests>=2.28.0",
    "flask~=2.0",
    "click==8.1.0",
    "pydantic",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black~=23.0",
]
